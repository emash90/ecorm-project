pipeline {
    agent any
    stages {
        stage('Build') {
            when {
                changeset "**/backend/AuthService/**"
            }
            steps {
                dir('backend/API-gateway') {
                    // Build steps for AuthService
                    sh 'docker build -t emash90/skin-backend-auth .'
                }
            }
        }
    }
}
